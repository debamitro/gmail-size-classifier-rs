[package]
name = "rust-gpui-app"
version = "0.1.0"
edition = "2021"

[dependencies]
gpui = { git = "https://github.com/zed-industries/zed" }
anyhow = "1.0"
async-trait = "0.1"
rocket = "0.5.0"
tokio = { version = "1.0", features = ["full"] }
